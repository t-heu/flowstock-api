{
  "version": 3,
  "builds": [
    {
      "src": "src/server.ts",
      "use": "@vercel/node",
      "config": { "distDir": "dist" }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/dist/server.js" }
  ]
}
